**{{ p.length }} consecutive points finishes.** {{ ins.subject }} scored in every race from {{ p.start.season }} {{ p.start.venue }} R{{ p.start.race }} to {{ p.end.season }} {{ p.end.venue }} R{{ p.end.race }} — {{ p.length }} starts without a blank.
