**Strongest car: {{ p.car }} — {{ '%.1f' | format(p.points_per_start) }} pts/start across {{ p.starts }} races.** {{ ins.subject }} produced {{ p.wins }} win{{ 's' if p.wins != 1 }} and {{ p.podiums }} podium{{ 's' if p.podiums != 1 }} in the car.
