**Class split: {{ p.leader }} podium rate {{ '%.1f' | format(p.leader_pct) }}% vs {{ p.other }} {{ '%.1f' | format(p.other_pct) }}%.** {{ ins.subject }} converts podiums noticeably more often in {{ p.leader }} machinery{% if p.ratio %} — {{ '%.1f' | format(p.ratio) }} times the rate{% endif %}.
