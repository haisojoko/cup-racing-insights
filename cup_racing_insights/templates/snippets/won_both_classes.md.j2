{%- if p.cohort_size == 1 -%}
**Only driver to win races in both Formula and Sports car formats.** {{ ins.subject }} has {{ p.formula_wins }} Formula win{{ 's' if p.formula_wins != 1 else '' }} and {{ p.sports_wins }} Sports car win{{ 's' if p.sports_wins != 1 else '' }} to their name.
{%- else -%}
**Race winner in both Formula and Sports car formats.** {{ ins.subject }} ({{ p.formula_wins }}F / {{ p.sports_wins }}S) is one of {{ p.cohort_size }} drivers in league history to take the checkered flag in both disciplines.
{%- endif -%}
