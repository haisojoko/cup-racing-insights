**{{ p.length }} consecutive top-{{ p.threshold }} finishes inside {{ p.season }}.** {{ ins.subject }}'s longest top-{{ p.threshold }} run within a single season, from {{ p.start.venue }} R{{ p.start.race }} to {{ p.end.venue }} R{{ p.end.race }}.
