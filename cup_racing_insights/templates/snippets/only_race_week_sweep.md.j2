{%- if p.cohort_size == 1 -%}
**Only driver in league history to sweep a race week.** {{ ins.subject }} has won every race in a venue weekend {{ p.sweep_count }} time{{ 's' if p.sweep_count != 1 }} — a clean sweep no one else has matched.
{%- else -%}
**{{ p.sweep_count }} race-week sweep{{ 's' if p.sweep_count != 1 }} (every race in a venue weekend won).** {{ ins.subject }} is one of {{ p.cohort_size }} drivers ever to do it.
{%- endif -%}
