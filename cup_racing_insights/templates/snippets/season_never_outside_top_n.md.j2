**Never outside the top {{ p.threshold }} in {{ p.season }}.** Every one of {{ ins.subject }}'s {{ p.starts }} starts finished inside the top {{ p.threshold }}, with a worst result of P{{ p.worst }}.
